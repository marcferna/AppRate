language: android

jdk:
 - openjdk8

android:
  components:
    - tools
    - build-tools-26.0.0
    - android-25
    - extra-google-m2repository
    - extra-android-m2repository

  licenses:
    - '.+'

script:
    - ./gradlew clean assemble --stacktrace

notifications:
  email: false

sudo: false

cache:
  directories:
    - $HOME/.m2
    - $HOME/.gradle
    - $ANDROID_HOME
